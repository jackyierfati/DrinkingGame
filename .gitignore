# 系统 / 编辑器
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# 依赖 / 构建（以后加了打包工具再用）
node_modules/
dist/
build/

# 日志
*.log

# 运行时生成的自定义词条（各自机器上的数据，不进仓库）
data/custom.json

# 词条提取的中间产物（视频 / 抽出来的帧，不进仓库）
frames/
*.mp4
*.mov
*.webm
*.mkv
